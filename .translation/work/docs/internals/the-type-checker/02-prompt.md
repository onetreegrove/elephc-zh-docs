Translate this elephc internals documentation into Simplified Chinese for Chinese-speaking developers.

- Use a concise technical documentation style.
- Preserve Markdown structure, tables, links, and all code fences exactly.
- Do not translate code blocks, inline identifiers, PHP/Rust symbols, file paths, URLs, CLI flags, API names, or elephc-specific names such as `elephc`, `EIR`, `FFI`, `extern`, `buffer<T>`, and `packed class`.
- Apply the project glossary in `.translation/glossary.md`; translate `type checker` as `类型检查器`, keep `AST`, `PhpType`, `ClassInfo`, `FunctionSig`, and Rust/PHP enum variants unchanged when they appear as identifiers.
- Translate frontmatter human-facing `title` and `description` values, but keep keys and `sidebar` order unchanged.
