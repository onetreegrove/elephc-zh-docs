---
title: "array_key_first()"
description: "通过共享的边界键辅助函数（selector 为 `0`）实现 `array_key_first()`。"
sidebar:
  order: 19
---

## array_key_first()

```php
function array_key_first(array $array): mixed
```

通过共享的边界键辅助函数（selector 为 `0`）实现 `array_key_first()`。

**参数**：
- `$array` (`array`)

**返回值**：`mixed`

_暂无示例 —— 请参阅 `examples/` 和 `showcases/` 目录中的用法模式。_




## 内部实现

有关 `array_key_first` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/array/array_key_first.md)。

