---
title: "array_pop()"
description: "通过修改长度并将 \`T|null\` 装箱为 Mixed，为索引数组下放 \`array_pop()\`。"
sidebar:
  order: 27
---

## array_pop()

```php
function array_pop(array $array): mixed
```

通过修改长度并将 `T|null` 装箱为 Mixed，为索引数组下放 `array_pop()`。

**参数**:
- `$array` (`array`)，引用传递

**返回值**: `mixed`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 以了解使用模式。_







## 内部实现

关于 `array_pop` 在编译器中的具体实现，请参阅[内部实现页面](../../../internals/builtins/array/array_pop.md)。
