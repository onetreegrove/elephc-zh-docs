---
title: "array_product()"
description: "在支持的索引数组负载上降级 `array_product()`。"
sidebar:
  order: 28
---

## array_product()

```php
function array_product(array $array): float
```

在支持的索引数组负载上降级 `array_product()`。

**参数**：
- `$array` (`array`)

**返回值**：`float`

_暂无示例——请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `array_product` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/array/array_product.md)。
