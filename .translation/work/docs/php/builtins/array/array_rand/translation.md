---
title: "array_rand()"
description: "针对索引数组降级 `array_rand()`。"
sidebar:
  order: 30
---

## array_rand()

```php
function array_rand(array $array, int $num): int
```

针对索引数组降级 `array_rand()`。

**参数**：
- `$array` (`array`)
- `$num` (`int`)

**返回值**：`int`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

有关 `array_rand` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/array/array_rand.md)。
