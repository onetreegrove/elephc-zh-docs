---
title: "array_replace_recursive()"
description: "降级 `array_replace_recursive()`（递归的右侧覆盖哈希合并）。"
sidebar:
  order: 33
---

## array_replace_recursive()

```php
function array_replace_recursive(array $array, array $replacements): mixed
```

降级 `array_replace_recursive()`（递归的右侧覆盖哈希合并）。

**参数**：
- `$array` (`array`)
- `$replacements` (`array`)

**返回值**：`mixed`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 了解使用模式。_







## 内部实现

关于编译器中如何实现 `array_replace_recursive`，请参阅[内部实现页面](../../../internals/builtins/array/array_replace_recursive.md)。
