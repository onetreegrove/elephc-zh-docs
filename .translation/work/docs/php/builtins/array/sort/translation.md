---
title: "sort()"
description: "通过原地修改源数组，为索引整数数组降级处理 `sort()`。"
sidebar:
  order: 58
---

## sort()

```php
function sort(array $array, int $flags): bool
```

通过原地修改源数组，为索引整数数组降级处理 `sort()`。

**参数**：
- `$array` (`array`)，引用传递
- `$flags` (`int`)

**返回值**：`bool`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `sort` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/array/sort.md)。
