---
title: "class_attribute_args()"
description: "将 `class_attribute_args(class, attr)` 降级为一个索引 Mixed 数组。"
sidebar:
  order: 65
---

## class_attribute_args()

```php
function class_attribute_args(string $class_name, string $attribute_name): array
```

将 `class_attribute_args(class, attr)` 降级为一个索引 Mixed 数组。

**参数**：
- `$class_name` (`string`)
- `$attribute_name` (`string`)

**返回值**：`array`

_暂无示例——请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `class_attribute_args` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/class/class_attribute_args.md)。
