---
title: "class_get_attributes()"
description: "将 `class_get_attributes(class)` 降级为 `ReflectionAttribute` 对象数组。"
sidebar:
  order: 68
---

## class_get_attributes()

```php
function class_get_attributes(string $class_name): mixed
```

将 `class_get_attributes(class)` 降级为 `ReflectionAttribute` 对象数组。

**参数**:
- `$class_name` (`string`)

**返回值**: `mixed`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 获取使用模式。_







## 内部实现

有关 `class_get_attributes` 在编译器中的具体实现，请参阅[内部实现页面](../../../internals/builtins/class/class_get_attributes.md)。
