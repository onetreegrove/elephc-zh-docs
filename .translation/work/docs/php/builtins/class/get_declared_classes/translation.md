---
title: "get_declared_classes()"
description: "get_declared_classes() — Elephc 支持的类内置函数。"
sidebar:
  order: 75
---

## get_declared_classes()

```php
function get_declared_classes(): array
```

`get_declared_classes()` 是 Elephc 支持的类内置函数。除非另有说明，其行为与 PHP 手册一致。

**参数**：无。

**返回值**：`array`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 获取使用模式。_
