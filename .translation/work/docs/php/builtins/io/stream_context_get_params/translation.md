---
title: "stream_context_get_params()"
description: "将 `stream_context_get_params(context)` 降级为一个空的关联哈希。"
sidebar:
  order: 192
---

## stream_context_get_params()

```php
function stream_context_get_params(resource $context): array
```

将 `stream_context_get_params(context)` 降级为一个空的关联哈希。

**参数**：
- `$context` (`resource`)

**返回值**：`array`

_暂无示例——请参阅 `examples/` 和 `showcases/` 中的使用模式。_




## 内部实现

有关 `stream_context_get_params` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/io/stream_context_get_params.md)。

