---
title: "json_last_error_msg()"
description: "通过运行时消息查找表降级 `json_last_error_msg()`。"
sidebar:
  order: 231
---

## json_last_error_msg()

```php
function json_last_error_msg(): string
```

通过运行时消息查找表降级 `json_last_error_msg()`。

**参数**：无。

**返回值**：`string`

_暂无示例 —— 请查阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

有关 `json_last_error_msg` 在编译器中是如何实现的，请参阅[内部实现页面](../../../internals/builtins/json/json_last_error_msg.md)。
