---
title: "clamp()"
description: "使用 PHP 兼容的边界检查转换数值 `clamp(value, min, max)` 调用。"
sidebar:
  order: 239
---

## clamp()

```php
function clamp(int $value, int $min, int $max): string
```

使用 PHP 兼容的边界检查转换数值 `clamp(value, min, max)` 调用。

**参数**：
- `$value` (`int`)
- `$min` (`int`)
- `$max` (`int`)

**返回值**：`string`

_暂无示例 —— 请查看 `examples/` 和 `showcases/` 获取使用模式。_






## 内部实现

关于 `clamp` 在编译器中的具体实现，请参阅[内部实现页面](../../../internals/builtins/math/clamp.md)。
