---
title: "cos()"
description: "cos() — Elephc 支持的数学内置函数。"
sidebar:
  order: 240
---

## cos()

```php
function cos(float $num): float
```

`cos()` 是 Elephc 支持的数学内置函数。除非另有说明，其行为与 PHP 手册一致。

**参数**：
- `$num` (`float`)

**返回值**：`float`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 获取使用模式。_
