---
title: "fmod()"
description: "针对具体的类整型和浮点型操作数下放 `fmod()`。"
sidebar:
  order: 246
---

## fmod()

```php
function fmod(float $num1, float $num2): float
```

针对具体的类整型和浮点型操作数下放 `fmod()`。

**参数**：
- `$num1` (`float`)
- `$num2` (`float`)

**返回值**：`float`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 了解使用模式。_







## 内部实现

关于 `fmod` 在编译器中的具体实现，请参见[内部实现页面](../../../internals/builtins/math/fmod.md)。
