---
title: "max()"
description: "针对具体的类整型和浮点型操作数下放数值型 `min()` 和 `max()`。"
sidebar:
  order: 255
---

## max()

```php
function max(mixed $value, ...$values): float
```

针对具体的类整型和浮点型操作数下放数值型 `min()` 和 `max()`。

**参数**：
- `$value` (`mixed`)
- `...$values` — 可变参数：将多余的参数收集到 `$values` 中。

**返回值**：`float`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 了解使用模式。_






## 内部实现

关于 `max` 在编译器中的具体实现，请参见[内部实现页面](../../../internals/builtins/math/max.md)。
