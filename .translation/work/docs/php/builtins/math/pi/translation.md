---
title: "pi()"
description: "将 `pi()` 降级为与遗留后端相同的数据段浮点常量。"
sidebar:
  order: 258
---

## pi()

```php
function pi(): float
```

将 `pi()` 降级为与遗留后端相同的数据段浮点常量。

**参数**：无。

**返回值**：`float`

_暂无示例 — 请检查 `examples/` 和 `showcases/` 以获取用法模式。_







## 内部实现

关于 `pi` 在编译器中的具体实现，请参见[内部实现页面](../../../internals/builtins/math/pi.md)。
