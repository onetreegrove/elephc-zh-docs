---
title: "pow()"
description: "针对具体的类整型和浮点型操作数下放 `pow()`。"
sidebar:
  order: 259
---

## pow()

```php
function pow(float $num, float $exponent): float
```

针对具体的类整型和浮点型操作数下放 `pow()`。

**参数**：
- `$num` (`float`)
- `$exponent` (`float`)

**返回值**：`float`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 了解使用模式。_







## 内部实现

关于 `pow` 在编译器中的具体实现，请参见[内部实现页面](../../../internals/builtins/math/pow.md)。
