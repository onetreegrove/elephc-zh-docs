---
title: "sqrt()"
description: "针对具体的类整型和浮点型操作数降级 \`sqrt()\`。"
sidebar:
  order: 266
---

## sqrt()

```php
function sqrt(float $num): float
```

针对具体的类整型和浮点型操作数降级 `sqrt()`。

**参数**：
- `$num` (`float`)

**返回值**：`float`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

有关 `sqrt` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/math/sqrt.md)。
