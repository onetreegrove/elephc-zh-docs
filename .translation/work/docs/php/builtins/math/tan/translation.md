---
title: "tan()"
description: "tan() — Elephc 支持的数学内置函数。"
sidebar:
  order: 267
---

## tan()

```php
function tan(float $num): float
```

`tan()` 是 Elephc 支持的数学内置函数。除非另有说明，否则其行为与 PHP 手册保持一致。

**参数**:
- `$num` (`float`)

**返回值**: `float`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 了解使用模式。_
