---
title: "ptr_read16()"
description: "降级 `ptr_read16(pointer)`：通过已检查的指针读取一个无符号 16 位字。"
sidebar:
  order: 290
---

## ptr_read16()

```php
function ptr_read16(pointer $pointer): int
```

降级 `ptr_read16(pointer)`：通过已检查的指针读取一个无符号 16 位字。

**参数**：
- `$pointer` (`pointer`)

**返回值**：`int`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

有关 `ptr_read16` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/pointer/ptr_read16.md)。
