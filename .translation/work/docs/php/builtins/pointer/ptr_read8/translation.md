---
title: "ptr_read8()"
description: "通过已检查的指针读取一个无符号字节来转换 `ptr_read8(pointer)`。"
sidebar:
  order: 292
---

## ptr_read8()

```php
function ptr_read8(pointer $pointer): int
```

通过已检查的指针读取一个无符号字节来转换 `ptr_read8(pointer)`。

**参数**:
- `$pointer` (`pointer`)

**返回值**: `int`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 以获取使用模式。_







## 内部实现

关于 `ptr_read8` 在编译器中的实现，请参见[内部实现页面](../../../internals/builtins/pointer/ptr_read8.md)。
