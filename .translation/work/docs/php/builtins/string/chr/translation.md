---
title: "chr()"
description: "通过将整数码位转换为单字节字符串来降级处理 `chr()`。"
sidebar:
  order: 339
---

## chr()

```php
function chr(int $codepoint): string
```

通过将整数码位转换为单字节字符串来降级处理 `chr()`。

**参数**：
- `$codepoint` (`int`)

**返回值**：`string`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `chr` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/string/chr.md)。
