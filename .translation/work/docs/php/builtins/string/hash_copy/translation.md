---
title: "hash_copy()"
description: "通过增量 hash 克隆辅助函数降级处理 `hash_copy(context)`。"
sidebar:
  order: 349
---

## hash_copy()

```php
function hash_copy(resource $context): mixed
```

通过增量 hash 克隆辅助函数降级处理 `hash_copy(context)`。

**参数**：
- `$context` (`resource`)

**返回值**：`mixed`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `hash_copy` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/string/hash_copy.md)。
