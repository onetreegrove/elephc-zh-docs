---
title: "floatval()"
description: "针对具体标量操作数降级处理 `floatval()`。"
sidebar:
  order: 410
---

## floatval()

```php
function floatval(mixed $value): float
```

针对具体标量操作数降级处理 `floatval()`。

**参数**：
- `$value` (`mixed`)

**返回值**：`float`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `floatval` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/type/floatval.md)。
