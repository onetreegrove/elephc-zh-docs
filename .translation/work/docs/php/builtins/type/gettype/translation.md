---
title: "gettype()"
description: "针对静态具体的 PHP 类型降级处理 `gettype(value)`。"
sidebar:
  order: 413
---

## gettype()

```php
function gettype(mixed $value): string
```

针对静态具体的 PHP 类型降级处理 `gettype(value)`。

**参数**：
- `$value` (`mixed`)

**返回值**：`string`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `gettype` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/type/gettype.md)。
