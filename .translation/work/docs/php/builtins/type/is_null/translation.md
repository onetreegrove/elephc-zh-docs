---
title: "is_null()"
description: "is_null() — Elephc 支持的 type 内置函数。"
sidebar:
  order: 421
---

## is_null()

```php
function is_null(mixed $value): bool
```

`is_null()` 是 Elephc 支持的 type 内置函数。除非下文另有说明，其行为与 PHP 手册一致。

**参数**：
- `$value` (`mixed`)

**返回值**：`bool`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







