---
title: "is_numeric()"
description: "针对具体标量值降级处理 `is_numeric()`。"
sidebar:
  order: 422
---

## is_numeric()

```php
function is_numeric(mixed $value): bool
```

针对具体标量值降级处理 `is_numeric()`。

**参数**：
- `$value` (`mixed`)

**返回值**：`bool`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `is_numeric` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/type/is_numeric.md)。
