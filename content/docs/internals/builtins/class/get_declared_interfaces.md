---
title: "get_declared_interfaces() — 内部实现"
description: "get_declared_interfaces() 的编译器内部实现：lowering 路径、类型检查以及运行时辅助函数。"
sidebar:
  order: 76
---

## `get_declared_interfaces()` — 内部实现

## 所在位置

- **签名**：[`src/types/signatures.rs`](https://github.com/illegalstudio/elephc/blob/main/src/types/signatures.rs)
- **Lowering**：[`(未 lowering)`:0]()
- **函数符号**：`(无 — 仅类型检查器)()`


## 运行时辅助函数

_未捕获到直接的 `__rt_*` 辅助函数 —— lowering 已内联或路由至另一个内置函数。_

## 签名摘要

```php
function get_declared_interfaces(): array
```

## 类型检查器约束

- **参数个数**：不接收参数。

## 交叉引用

- [`get_declared_interfaces()` 用户参考](../../../php/builtins/class/get_declared_interfaces.md)
