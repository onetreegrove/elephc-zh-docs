---
title: "array_all()"
description: "降级 `array_all()`：当数组中每个元素都满足谓词时返回 true。"
sidebar:
  order: 1
---

## array_all()

```php
function array_all(array $array, mixed $callback): mixed
```

降级 `array_all()`：当数组中每个元素都满足谓词时返回 true。

**参数**：
- `$array` (`array`)
- `$callback` (`mixed`)

**返回值**：`mixed`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_




## 内部实现

有关 `array_all` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/array/array_all.md)。
