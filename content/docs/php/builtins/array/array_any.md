---
title: "array_any()"
description: "降低 `array_any()`：当某个元素满足谓词条件时返回 true。"
sidebar:
  order: 2
---

## array_any()

```php
function array_any(array $array, mixed $callback): mixed
```

降低 `array_any()`：当某个元素满足谓词条件时返回 true。

**参数**：
- `$array` (`array`)
- `$callback` (`mixed`)

**返回值**：`mixed`

_暂无示例——请查阅 `examples/` 和 `showcases/` 目录以了解用法。_




## 内部实现

关于 `array_any` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/array/array_any.md)。

