---
title: "array_flip()"
description: "通过旧版哈希构建运行时辅助函数降级处理 `array_flip()`。"
sidebar:
  order: 13
---

## array_flip()

```php
function array_flip(array $array): float
```

通过旧版哈希构建运行时辅助函数降级处理 `array_flip()`。

**参数**：
- `$array` (`array`)

**返回值**：`float`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录中的使用模式。_




## 内部实现

关于 `array_flip` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/array/array_flip.md)。

