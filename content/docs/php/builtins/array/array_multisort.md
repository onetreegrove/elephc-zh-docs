---
title: "array_multisort()"
description: "降级 `array_multisort()`：对第一个索引数组进行升序稳定排序，并重新排列第二个数组"
sidebar:
  order: 25
---

## array_multisort()

```php
function array_multisort(array $array1, int $array2): bool
```

降级 `array_multisort()`：对第一个索引数组进行升序稳定排序，并重新排列第二个数组

**参数**：
- `$array1` (`array`)
- `$array2` (`int`)

**返回值**：`bool`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_






## 内部实现

有关 `array_multisort` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/array/array_multisort.md)。


