---
title: "array_pad()"
description: "通过复制索引数组并填充缺失槽位来降级处理 `array_pad()`。"
sidebar:
  order: 26
---

## array_pad()

```php
function array_pad(array $array, int $length, mixed $value): array
```

通过复制索引数组并填充缺失槽位来降级处理 `array_pad()`。

**参数**：
- `$array` (`array`)
- `$length` (`int`)
- `$value` (`mixed`)

**返回值**：`array`

_暂无示例——请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_






## 内部实现

关于 `array_pad` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/array/array_pad.md)。
