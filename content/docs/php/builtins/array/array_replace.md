---
title: "array_replace()"
description: "降级 `array_replace()`（两个哈希的右侧覆盖合并）。"
sidebar:
  order: 32
---

## array_replace()

```php
function array_replace(array $array, array $replacements): mixed
```

降级 `array_replace()`（两个哈希的右侧覆盖合并）。

**参数**：
- `$array` (`array`)
- `$replacements` (`array`)

**返回值**：`mixed`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 了解使用模式。_







## 内部实现

关于编译器中如何实现 `array_replace`，请参阅[内部实现页面](../../../internals/builtins/array/array_replace.md)。
