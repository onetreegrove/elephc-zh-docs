---
title: "arsort()"
description: "通过降序值排序包装器，为索引整数数组降级处理 `arsort()`。"
sidebar:
  order: 47
---

## arsort()

```php
function arsort(array $array, int $flags): bool
```

通过降序值排序包装器，为索引整数数组降级处理 `arsort()`。

**参数**：
- `$array` (`array`)，引用传递
- `$flags` (`int`)

**返回值**：`bool`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `arsort` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/array/arsort.md)。

