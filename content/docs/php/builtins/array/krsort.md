---
title: "krsort()"
description: "通过遗留的反向键排序辅助接口下放 \`krsort()\`。"
sidebar:
  order: 51
---

## krsort()

```php
function krsort(array $array, int $flags): bool
```

通过遗留的反向键排序辅助接口下放 `krsort()`。

**参数**:
- `$array` (`array`)，引用传递
- `$flags` (`int`)

**返回值**: `bool`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 以了解使用模式。_







## 内部实现

关于 `krsort` 在编译器中的具体实现，请参阅[内部实现页面](../../../internals/builtins/array/krsort.md)。
