---
title: "class_attribute_names()"
description: "将 `class_attribute_names(class)` 降级为索引字符串数组。"
sidebar:
  order: 66
---

## class_attribute_names()

```php
function class_attribute_names(string $class_name): array
```

将 `class_attribute_names(class)` 降级为索引字符串数组。

**参数**:
- `$class_name` (`string`)

**返回值**: `array`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 获取使用模式。_






## 内部实现

有关 `class_attribute_names` 在编译器中的具体实现，请参阅[内部实现页面](../../../internals/builtins/class/class_attribute_names.md)。
