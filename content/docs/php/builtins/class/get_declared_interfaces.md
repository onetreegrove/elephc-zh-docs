---
title: "get_declared_interfaces()"
description: "get_declared_interfaces() — Elephc 支持的类内置函数。"
sidebar:
  order: 76
---

## get_declared_interfaces()

```php
function get_declared_interfaces(): array
```

`get_declared_interfaces()` 是 Elephc 支持的类内置函数。除非另有说明，其行为与 PHP 手册一致。

**参数**：无。

**返回值**：`array`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 获取使用模式。_
