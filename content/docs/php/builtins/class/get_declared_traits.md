---
title: "get_declared_traits()"
description: "get_declared_traits() — Elephc 支持的类内置函数。"
sidebar:
  order: 77
---

## get_declared_traits()

```php
function get_declared_traits(): array
```

`get_declared_traits()` 是 Elephc 支持的类内置函数。除非下文另有说明，否则其行为与 PHP 手册一致。

**参数**：无。

**返回值**：`array`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 以获取使用模式。_
