---
title: "date_default_timezone_get()"
description: "通过共享的运行时辅助函数降低 `date_default_timezone_get()`。"
sidebar:
  order: 85
---

## date_default_timezone_get()

```php
function date_default_timezone_get(): string
```

通过共享的运行时辅助函数降低 `date_default_timezone_get()`。

**参数**：无。

**返回值**：`string`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 获取使用模式。_







## 内部机制

关于 `date_default_timezone_get` 在编译器中的实现方式，请参阅[内部机制页面](../../../internals/builtins/date/date_default_timezone_get.md)。
