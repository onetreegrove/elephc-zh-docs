---
title: "stream_context_create()"
description: "降级处理 `stream_context_create(options?, params?)`。"
sidebar:
  order: 189
---

## stream_context_create()

```php
function stream_context_create(array $options, array $params): mixed
```

降级处理 `stream_context_create(options?, params?)`。

**参数**：
- `$options` (`array`)，可选
- `$params` (`array`)，可选

**返回值**：`mixed`

_暂无示例——请参阅 `examples/` 和 `showcases/` 目录以了解使用方式。_




## 内部实现

关于 `stream_context_create` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/io/stream_context_create.md)。
