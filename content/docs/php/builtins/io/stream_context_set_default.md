---
title: "stream_context_set_default()"
description: "将 `stream_context_set_default(options)` 降级处理。"
sidebar:
  order: 193
---

## stream_context_set_default()

```php
function stream_context_set_default(array $options): mixed
```

将 `stream_context_set_default(options)` 降级处理。

**参数**：
- `$options` (`array`)

**返回值**：`mixed`

_暂无示例——请参阅 `examples/` 和 `showcases/` 目录中的用法模式。_




## 内部实现

有关 `stream_context_set_default` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/io/stream_context_set_default.md)。

