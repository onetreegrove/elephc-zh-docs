---
title: "abs()"
description: "针对具体的类整数和浮点操作数，降级 `abs()`。"
sidebar:
  order: 233
---

## abs()

```php
function abs(int $num): mixed
```

针对具体的类整数和浮点操作数，降级 `abs()`。

**参数**：
- `$num` (`int`)

**返回值**：`mixed`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

有关 `abs` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/math/abs.md)。


