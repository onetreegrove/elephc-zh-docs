---
title: "ceil()"
description: "针对具体的类整数和浮点操作数下放 `ceil()`。"
sidebar:
  order: 238
---

## ceil()

```php
function ceil(float $num): float
```

针对具体的类整数和浮点操作数下放 `ceil()`。

**参数**:
- `$num` (`float`)

**返回值**: `float`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 以获取使用模式。_






## 内部实现

关于 `ceil` 在编译器中的具体实现，请参见[内部实现页面](../../../internals/builtins/math/ceil.md)。
