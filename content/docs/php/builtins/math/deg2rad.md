---
title: "deg2rad()"
description: "通过乘以 `PI / 180` 来降级 `deg2rad()`。"
sidebar:
  order: 242
---

## deg2rad()

```php
function deg2rad(float $num): float
```

通过乘以 `PI / 180` 来降级 `deg2rad()`。

**参数**：
- `$num` (`float`)

**返回值**：`float`

_暂无示例 —— 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

有关 `deg2rad` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/math/deg2rad.md)。
