---
title: "hypot()"
description: "使用 C ABI 参数顺序 `x, y` 下放 `hypot()`。"
sidebar:
  order: 247
---

## hypot()

```php
function hypot(float $x, float $y): float
```

使用 C ABI 参数顺序 `x, y` 下放 `hypot()`。

**参数**：
- `$x` (`float`)
- `$y` (`float`)

**返回值**：`float`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 了解使用模式。_






## 内部实现

关于 `hypot` 在编译器中的具体实现，请参见[内部实现页面](../../../internals/builtins/math/hypot.md)。
