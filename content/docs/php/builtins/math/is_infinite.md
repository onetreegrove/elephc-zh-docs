---
title: "is_infinite()"
description: "通过将归一化后的浮点数与正负无穷大进行比较来下放 `is_infinite()`。"
sidebar:
  order: 250
---

## is_infinite()

```php
function is_infinite(float $num): bool
```

通过将归一化后的浮点数与正负无穷大进行比较来下放 `is_infinite()`。

**参数**：
- `$num` (`float`)

**返回值**：`bool`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 了解使用模式。_






## 内部实现

关于 `is_infinite` 在编译器中的具体实现，请参见[内部实现页面](../../../internals/builtins/math/is_infinite.md)。
