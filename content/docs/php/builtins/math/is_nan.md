---
title: "is_nan()"
description: "通过检查规范化后的 float 是否与自身无序来下放 `is_nan()`。"
sidebar:
  order: 251
---

## is_nan()

```php
function is_nan(float $num): bool
```

通过检查规范化后的 float 是否与自身无序来下放 `is_nan()`。

**参数**：
- `$num` (`float`)

**返回值**：`bool`

_暂无示例 —— 请查看 `examples/` 和 `showcases/` 了解使用模式。_







## 内部实现

关于 `is_nan` 在编译器中的具体实现，请参阅[内部实现页面](../../../internals/builtins/math/is_nan.md)。
