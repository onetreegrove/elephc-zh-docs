---
title: "log()"
description: "下放单参数和换底的双参数形式的 `log()`。"
sidebar:
  order: 252
---

## log()

```php
function log(float $num, float $base): float
```

下放单参数和换底的双参数形式的 `log()`。

**参数**：
- `$num` (`float`)
- `$base` (`float`)，可选

**返回值**：`float`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 了解使用模式。_







## 内部实现

有关 `log` 在编译器中的具体实现，请参阅 [内部实现页面](../../../internals/builtins/math/log.md)。
