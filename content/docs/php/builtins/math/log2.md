---
title: "log2()"
description: "log2() — Elephc 支持的数学内置函数。"
sidebar:
  order: 254
---

## log2()

```php
function log2(float $num): float
```

`log2()` 是 Elephc 支持的数学内置函数。除非另有说明，其行为与 PHP 手册一致。

**参数**：
- `$num` (`float`)

**返回值**：`float`

_暂无示例 — 请检查 `examples/` 和 `showcases/` 以了解使用模式。_
