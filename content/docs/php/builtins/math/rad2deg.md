---
title: "rad2deg()"
description: "通过乘以 `180 / PI` 来降级 `rad2deg()`。"
sidebar:
  order: 260
---

## rad2deg()

```php
function rad2deg(float $num): float
```

通过乘以 `180 / PI` 来降级 `rad2deg()`。

**参数**：
- `$num` (`float`)

**返回值**：`float`

_暂无示例 —— 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

有关 `rad2deg` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/math/rad2deg.md)。


