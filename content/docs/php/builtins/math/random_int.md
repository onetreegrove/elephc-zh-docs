---
title: "random_int()"
description: "在包含端点的整数区间内下放 `random_int()`。"
sidebar:
  order: 262
---

## random_int()

```php
function random_int(int $min, int $max): int
```

在包含端点的整数区间内下放 `random_int()`。

**参数**:
- `$min` (`int`)
- `$max` (`int`)

**返回值**: `int`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 以了解使用模式。_







## 内部实现

关于 `random_int` 在编译器中的具体实现，请参阅[内部实现页面](../../../internals/builtins/math/random_int.md)。
