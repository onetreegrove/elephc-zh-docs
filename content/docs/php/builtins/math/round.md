---
title: "round()"
description: "针对具体的类整型和浮点操作数下放 `round()`。"
sidebar:
  order: 263
---

## round()

```php
function round(float $num, int $precision): float
```

针对具体的类整型和浮点操作数下放 `round()`。

**参数**:
- `$num` (`float`)
- `$precision` (`int`)，可选

**返回值**: `float`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 以了解使用模式。_







## 内部实现

关于 `round` 在编译器中的具体实现，请参阅[内部实现页面](../../../internals/builtins/math/round.md)。
