---
title: "sin()"
description: "sin() —— Elephc 支持的数学内置函数。"
sidebar:
  order: 264
---

## sin()

```php
function sin(float $num): float
```

`sin()` 是 Elephc 支持的数学内置函数。除非另有说明，其行为与 PHP 手册一致。

**参数**:
- `$num` (`float`)

**返回值**: `float`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 以了解使用模式。_
