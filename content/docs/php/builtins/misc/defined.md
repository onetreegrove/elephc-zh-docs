---
title: "defined()"
description: "检查给定的命名常量是否存在。"
sidebar:
  order: 273
---

## defined()

```php
function defined(string $constant_name): bool
```

检查给定的命名常量是否存在。

**参数**:
- `$constant_name` (`string`)

**返回值**: `bool`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 了解使用模式。_







## 内部实现

关于 `defined` 在编译器中是如何实现的，请参见 [内部实现页面](../../../internals/builtins/misc/defined.md)。
