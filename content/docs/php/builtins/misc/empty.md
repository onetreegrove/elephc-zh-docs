---
title: "empty()"
description: "检查变量是否被视为空。"
sidebar:
  order: 274
---

## empty()

```php
function empty(mixed $value): bool
```

检查变量是否被视为空。

**参数**:
- `$value` (`mixed`)

**返回值**: `bool`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 以了解用法。_







## 内部实现

关于 `empty` 在编译器中是如何实现的，请参见[内部实现页面](../../../internals/builtins/misc/empty.md)。
