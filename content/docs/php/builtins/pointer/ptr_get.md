---
title: "ptr_get()"
description: "降级 `ptr_get(pointer)`：通过已检查的指针读取一个机器字。"
sidebar:
  order: 286
---

## ptr_get()

```php
function ptr_get(pointer $pointer): int
```

降级 `ptr_get(pointer)`：通过已检查的指针读取一个机器字。

**参数**：
- `$pointer` (`pointer`)

**返回值**：`int`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

有关 `ptr_get` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/pointer/ptr_get.md)。
