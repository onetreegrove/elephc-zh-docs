---
title: "ptr_read32()"
description: "通过已检查的指针读取一个无符号 32 位字来降级 `ptr_read32(pointer)`。"
sidebar:
  order: 291
---

## ptr_read32()

```php
function ptr_read32(pointer $pointer): int
```

通过已检查的指针读取一个无符号 32 位字来降级 `ptr_read32(pointer)`。

**参数**:
- `$pointer` (`pointer`)

**返回值**: `int`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 获取用法参考。_







## 内部实现

关于 `ptr_read32` 在编译器中的具体实现，请参阅[内部实现页面](../../../internals/builtins/pointer/ptr_read32.md)。
