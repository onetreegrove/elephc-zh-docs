---
title: "ptr_sizeof()"
description: "通过具体化已检查的静态字节大小来下放 `ptr_sizeof(\"type\")`。"
sidebar:
  order: 295
---

## ptr_sizeof()

```php
function ptr_sizeof(string $type): mixed
```

通过具体化已检查的静态字节大小来下放 `ptr_sizeof("type")`。

**参数**：
- `$type` (`string`)

**返回值**：`mixed`

_暂无示例 — 请查看 `examples/` 和 `showcases/` 以了解使用模式。_







## 内部实现

关于 `ptr_sizeof` 在编译器中是如何实现的，请参见[内部实现页面](../../../internals/builtins/pointer/ptr_sizeof.md)。
