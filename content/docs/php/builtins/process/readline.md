---
title: "readline()"
description: "降级处理 `readline(prompt?)`：可选地写入提示符并读取 stdin。"
sidebar:
  order: 306
---

## readline()

```php
function readline(string $prompt): mixed
```

降级处理 `readline(prompt?)`：可选地写入提示符并读取 stdin。

**参数**：
- `$prompt` (`string`)，可选

**返回值**：`mixed`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `readline` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/process/readline.md)。
