---
title: "iterator_count()"
description: "在数组、`iterable` 和 Traversable 对象上降级处理 `iterator_count()`。"
sidebar:
  order: 317
---

## iterator_count()

```php
function iterator_count(traversable $iterator): int
```

在数组、`iterable` 和 Traversable 对象上降级处理 `iterator_count()`。

**参数**：
- `$iterator` (`traversable`)

**返回值**：`int`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `iterator_count` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/spl/iterator_count.md)。
