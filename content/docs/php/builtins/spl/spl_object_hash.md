---
title: "spl_object_hash()"
description: "通过将已加载的对象指针格式化为字符串来降级处理 `spl_object_hash(object)`。"
sidebar:
  order: 326
---

## spl_object_hash()

```php
function spl_object_hash(object $object): string
```

通过将已加载的对象指针格式化为字符串来降级处理 `spl_object_hash(object)`。

**参数**：
- `$object` (`object`)

**返回值**：`string`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `spl_object_hash` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/spl/spl_object_hash.md)。
