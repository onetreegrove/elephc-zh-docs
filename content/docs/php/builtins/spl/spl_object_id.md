---
title: "spl_object_id()"
description: "通过将已加载的对象指针作为整数返回来降级处理 `spl_object_id(object)`。"
sidebar:
  order: 327
---

## spl_object_id()

```php
function spl_object_id(object $object): int
```

通过将已加载的对象指针作为整数返回来降级处理 `spl_object_id(object)`。

**参数**：
- `$object` (`object`)

**返回值**：`int`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `spl_object_id` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/spl/spl_object_id.md)。
