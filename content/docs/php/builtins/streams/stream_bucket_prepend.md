---
title: "stream_bucket_prepend()"
description: "stream_bucket_prepend() — Elephc 支持的 streams 内置函数。"
sidebar:
  order: 331
---

## stream_bucket_prepend()

```php
function stream_bucket_prepend(mixed $brigade, mixed $bucket): void
```

`stream_bucket_prepend()` 是 Elephc 支持的 streams 内置函数。除非下文另有说明，其行为与 PHP 手册一致。

**参数**：
- `$brigade` (`mixed`)
- `$bucket` (`mixed`)

**返回值**：`void`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







