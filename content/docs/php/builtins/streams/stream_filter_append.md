---
title: "stream_filter_append()"
description: "stream_filter_append() — Elephc 支持的 streams 内置函数。"
sidebar:
  order: 332
---

## stream_filter_append()

```php
function stream_filter_append(resource $stream, string $filter_name, int $mode, mixed $params): mixed
```

`stream_filter_append()` 是 Elephc 支持的 streams 内置函数。除非下文另有说明，其行为与 PHP 手册一致。

**参数**：
- `$stream` (`resource`)
- `$filter_name` (`string`)
- `$mode` (`int`)，可选
- `$params` (`mixed`)，可选

**返回值**：`mixed`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







