---
title: "grapheme_strrev()"
description: "降级处理 `grapheme_strrev()`，并将其 `string|false` 结果装箱为 `Mixed`。"
sidebar:
  order: 342
---

## grapheme_strrev()

```php
function grapheme_strrev(string $string): mixed
```

降级处理 `grapheme_strrev()`，并将其 `string|false` 结果装箱为 `Mixed`。

**参数**：
- `$string` (`string`)

**返回值**：`mixed`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `grapheme_strrev` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/string/grapheme_strrev.md)。
