---
title: "hash_algos()"
description: "通过运行时算法列表构建器降级处理 `hash_algos()`。"
sidebar:
  order: 348
---

## hash_algos()

```php
function hash_algos(): array
```

通过运行时算法列表构建器降级处理 `hash_algos()`。

**参数**：无。

**返回值**：`array`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `hash_algos` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/string/hash_algos.md)。
