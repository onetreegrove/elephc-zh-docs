---
title: "lcfirst()"
description: "通过复制字符串并将第一个 ASCII 字节转为小写来降级处理 `lcfirst()`。"
sidebar:
  order: 363
---

## lcfirst()

```php
function lcfirst(string $string): string
```

通过复制字符串并将第一个 ASCII 字节转为小写来降级处理 `lcfirst()`。

**参数**：
- `$string` (`string`)

**返回值**：`string`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `lcfirst` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/string/lcfirst.md)。
