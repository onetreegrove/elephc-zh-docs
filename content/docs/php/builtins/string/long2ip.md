---
title: "long2ip()"
description: "通过 IPv4 格式化运行时辅助函数降级处理 `long2ip(value)`。"
sidebar:
  order: 364
---

## long2ip()

```php
function long2ip(int $ip): string
```

通过 IPv4 格式化运行时辅助函数降级处理 `long2ip(value)`。

**参数**：
- `$ip` (`int`)

**返回值**：`string`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `long2ip` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/string/long2ip.md)。
