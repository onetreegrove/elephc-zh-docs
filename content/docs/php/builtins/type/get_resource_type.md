---
title: "get_resource_type()"
description: "将 `get_resource_type(resource)` 降级为 elephc 当前的 resource 类型标签。"
sidebar:
  order: 412
---

## get_resource_type()

```php
function get_resource_type(resource $resource): string
```

将 `get_resource_type(resource)` 降级为 elephc 当前的 resource 类型标签。

**参数**：
- `$resource` (`resource`)

**返回值**：`string`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `get_resource_type` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/type/get_resource_type.md)。
