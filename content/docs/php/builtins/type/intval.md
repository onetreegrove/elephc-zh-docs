---
title: "intval()"
description: "针对具体标量操作数降级处理 `intval()`。"
sidebar:
  order: 414
---

## intval()

```php
function intval(mixed $value, int $base): int
```

针对具体标量操作数降级处理 `intval()`。

**参数**：
- `$value` (`mixed`)
- `$base` (`int`)

**返回值**：`int`

_暂无示例 — 请参阅 `examples/` 和 `showcases/` 目录以了解使用模式。_







## 内部实现

关于 `intval` 在编译器中的实现方式，请参阅[内部实现页面](../../../internals/builtins/type/intval.md)。
